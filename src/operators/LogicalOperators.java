package operators;

public class LogicalOperators {
    public static void main(String[] args) {
         // Logical Operators
         // (&) : AND
         // (|) : OR
         // (!) : NOT
         // (^) : XOR (exclusive OR)
         // (||) : Short-circuit OR
         // (&&) : Short-circuit AND

    }
    
}
